# Core data generation + toolkit (required)
pandas>=2.0
numpy>=1.24

# Control Tower frontend (app/server.py)
fastapi>=0.110
uvicorn[standard]>=0.29

# LangGraph agent layer (required ONLY for agents/graph.py — the LLM agents)
# The deterministic orchestrator (agents/pipeline.py) needs neither of these.
langgraph>=0.2
langchain-anthropic>=0.2
langchain-core>=0.3
